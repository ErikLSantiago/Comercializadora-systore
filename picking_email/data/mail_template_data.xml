<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="0">
    <record id="mail_template_picking_processed_default" model="mail.template">
        <field name="name">Operaciones: Pedido procesado</field>
        <field name="model_id" ref="stock.model_stock_picking"/>
        <field name="is_favorite">True</field>
        <field name="subject">Tu pedido ${object.name} ha sido procesado</field>
        <field name="email_from">${(object.company_id.email_formatted or user.email_formatted) if object.company_id else user.email_formatted}</field>
        <field name="email_to">${object.partner_id.email or ''}</field>
        <field name="auto_delete">True</field>
        <field name="lang">${object.partner_id.lang or user.lang}</field>
        <field name="body_html"><![CDATA[
<div>
    <p>Hola ${object.partner_id.name or 'cliente'},</p>
    <p>Te informamos que tu pedido relacionado con la operación de almacén <strong>${object.name}</strong> ha sido <strong>procesado</strong>.</p>
    % if object.origin:
    <p>Referencia: ${object.origin}</p>
    % endif
    <p>Estado: ${object.state}</p>
    % if object.scheduled_date or object.date or object.create_date:
    <p>Fecha: ${format_date(object.scheduled_date or object.date or object.create_date)}</p>
    % endif
    % if object.move_ids_without_package:
    <p>Artículos:</p>
    <ul>
        % for move in object.move_ids_without_package:
        <li>${move.product_id.display_name} — ${move.product_uom_qty} ${move.product_uom.name}</li>
        % endfor
    </ul>
    % endif
    <p>Gracias por tu preferencia,<br/>${object.company_id.name}</p>
</div>
        ]]></field>
    </record>
</odoo>