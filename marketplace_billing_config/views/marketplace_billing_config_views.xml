<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_marketplace_billing_config_list" model="ir.ui.view">
        <field name="name">marketplace.billing.config.list</field>
        <field name="model">marketplace.billing.config</field>
        <field name="arch" type="xml">
            <list string="Marketplace: Facturación por canal" editable="bottom">
                <field name="company_id"/>
                <field name="channel_binding_id"/>
                <field name="code" readonly="1"/>
                <field name="invoice_partner_id"/>
                <field name="income_account_id"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <record id="view_marketplace_billing_config_form" model="ir.ui.view">
        <field name="name">marketplace.billing.config.form</field>
        <field name="model">marketplace.billing.config</field>
        <field name="arch" type="xml">
            <form string="Marketplace: Facturación por canal">
                <sheet>
                    <group>
                        <group>
                            <field name="company_id"/>
                            <field name="channel_binding_id"/>
                            <field name="code" readonly="1"/>
                        </group>
                        <group>
                            <field name="invoice_partner_id"/>
                            <field name="income_account_id"/>
                            <field name="active"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_marketplace_billing_config" model="ir.actions.act_window">
        <field name="name">Marketplace: Facturación por canal</field>
        <field name="res_model">marketplace.billing.config</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_active': 1}</field>
    </record>

    <menuitem id="menu_marketplace_billing_root"
              name="Marketplaces"
              parent="sale.sale_menu_root"
              sequence="65"/>

    <menuitem id="menu_marketplace_billing_config"
              name="Facturación por canal"
              parent="menu_marketplace_billing_root"
              action="action_marketplace_billing_config"
              sequence="10"/>

</odoo>
